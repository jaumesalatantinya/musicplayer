import { expect } from "chai";
import { spy } from 'sinon';
import App from './App';


describe('App Tests', () => {


    describe('Constructor', () => {
        it('Should empty inicialize Music Player', () => {
        });
    });

   describe('Init', () => {

        it('Should call getSectionsFromRenderedAccordion getSectionsFromApi', () => {
        });
    });
});
